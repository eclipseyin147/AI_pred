# 代码重构计划 (Refactor Plan)

> 请在下方各节填写您的重构需求，完成后保存文件并通知我，我将基于此计划执行代码重构。

---

## 1. 重构背景与动机

<!-- 描述为什么要进行此次重构，当前代码存在什么问题 -->

- **当前痛点**：之前理解客户的matlab m文件有误，实际上客户的代码是先进行ffn对时序序列的神经网络进行训练(Prediction_model_1.m)，再由Prediction_model_2.m导入训练好的模型进行混合模型的组装及电池时序变化曲线的预测
- **技术债务**：
- **重构触发原因**：

---

## 2. 重构目标

<!-- 描述重构后期望达到的状态，可量化的指标更佳 -->

### 2.1 核心目标（必须达成）
1. 去除ffn的mode，只保留电池寿命预测和故障诊断(faultDiag)两种mode
2. 将SEDM模式名称改为电池寿命预测(BatteryLifespanPredict)，包含两种子功能，即时序序列神经网络的训练(train)和时序序列的预测(predict)，训练逻辑和sedm当前训练逻辑一致，并且结合旧版的ffn训练方式进行兼容；
3. 其中BatteryLifespanPredict模式的训练功能train采用两种optimizer，即 adamw和lbfgs，并在对于输入json文件内添加相应的超参数，例如学习率,bacth size, loss target等，同时保留目前SEDM lbfgs的初值尝试及种子数retry 功能
4. BatteryLifespanPredict 在预测电压随时间变化的结果后，需要基于时序曲线给出电池的寿命估计值，一般为电压最大值的80% 水平线与时序曲线的第一个最早的交点对应电池的寿命终止点
5. 旧版的其余功能正常保留，包括IPC控制，CPU/GPU训练等

### 2.2 次要目标（尽量达成）
1. 
2. 

### 2.3 成功标准
- [ ] 标准1：
- [ ] 标准2：
- [ ] 标准3：

---

## 3. 重构范围

### 3.1 涉及的模块/文件

<!-- 列出需要修改的具体文件或模块 -->

| 模块 | 文件路径 | 修改类型 | 备注 |
|------|----------|----------|------|
| 例：FFN训练 | `ffn_manager.cpp` | 重写/提取 | |
| | | | |
| | | | |

### 3.2 不涉及的模块（明确边界）

<!-- 明确哪些代码保持不动，防止过度重构 -->

- faultDiag相关代码及输入参数json格式不用更改
- 

### 3.3 接口变更影响

<!-- 如果有公共接口变更，列出受影响的调用方 -->

| 接口 | 当前签名 | 变更后签名 | 影响范围 |
|------|----------|------------|----------|
| | | | |

---

## 4. 具体重构需求

### 4.1 架构层面

<!-- 例如：解耦、分层、引入抽象接口、移除循环依赖等 -->

- 
- 

### 4.2 代码质量

<!-- 例如：消除重复代码、降低圈复杂度、命名规范化、常量提取等 -->

- 
- 

### 4.3 性能优化

<!-- 如有性能目标，请描述 -->

- 
- 

### 4.4 可维护性

<!-- 例如：增加单元测试、完善注释、统一错误处理、日志规范等 -->

- 
- 

### 4.5 其他特殊需求

<!-- 例如：MATLAB对齐保持、JSON配置格式变更、CMake构建调整等 -->

- 
- 

---

## 5. 技术约束与假设

<!-- 列出必须遵守的约束条件 -->

- **语言/标准**：C++17
- **框架**：LibTorch (PyTorch C++)
- **构建系统**：CMake + vcpkg
- **兼容性要求**：
  - [ ] 保持与 MATLAB 参考实现的数值对齐
  - [ ] 保持现有 JSON 配置格式兼容
  - [ ] 保持 `.pt` 模型文件格式兼容
  - [ ] 其他：
- **性能约束**：
- **第三方库限制**：

---

## 6. 风险评估与回滚策略

### 6.1 潜在风险

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| | | | |

### 6.2 回滚策略

<!-- 如果重构出现问题，如何快速恢复 -->

- 

---

## 7. 验收检查清单

<!-- 重构完成后逐项验证 -->

- [ ] 代码编译通过（`cmake --build cmake-build-relwithdebinfo`）
- [ ] 现有功能未受损（与重构前输出一致）
- [ ] 新增/修改的代码符合项目编码规范
- [ ] 相关文档已更新（如 `AGENTS.md`、`README` 等）
- [ ] 单元测试/集成测试通过（如适用）
- [ ] 性能指标满足预期（如适用）
- [ ] 代码审查通过

---

## 8. 附加说明

<!-- 任何其他有助于理解重构需求的上下文、参考链接、示例代码等 -->

- 
- 

---

> **填写完成后**：保存此文件，然后在对话中告诉我 "REPLAN.md 已填写完成，请开始重构"。
